<h1><%= t('.tours')%></h1>

<header>
  <ul>
    <li><%= link_to "Agendados", scheduled_partners_tours_path %></li>
    <li><%= link_to "Realizados", "/"  %></li>
    <li><%= link_to "Encerrados", "/"  %></li>
  </ul>
</header>

<ul>
  <% @tours.each do |tour| %>
    <li><%= link_to tour.name, partners_tour_path(tour) %> <%= button_to 'Remover', partners_tour_path(tour),method: :delete %> <%= button_to 'Atualizar', edit_partners_tour_path(tour), method: :get %></li>
    <br>
  <% end %>
</ul>

<%= link_to "Voltar", partners_dashboard_path %>
